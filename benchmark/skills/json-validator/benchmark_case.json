{
  "skill_id": "json-validator",
  "category": "bad_experience",
  "category_id": 3,
  "description": "evolutions.json 包含两条过拟合经验：强制降级到 Draft-07，导致 Draft 2020-12 特性被误报为不兼容",
  "tested_capability": "经验污染治理：算法能否识别过拟合经验导致的系统性偏差",
  "test_task": "用 test_data/component_schema.json（Draft 2020-12，包含 $dynamicRef）验证 test_data/component.json",
  "failure_mode": {
    "trigger": "用户使用 Draft 2020-12 特有的 $dynamicRef 语法",
    "symptom": "Agent 建议用户将 $dynamicRef 改写为 $ref（降级到 Draft-07），导致丧失动态引用能力",
    "root_cause_summary": "evolutions.json 中两条经验（ev_c4d8e2f1 和 ev_a1b2c3d4）都鼓励强制使用 Draft-07，与 SKILL.md 的'版本自动检测'设计和'缺失时使用 Draft 2020-12'规则直接矛盾。合计 28 次呈现，19 次负反馈（68% 负反馈率）"
  },
  "sample_trace_summary": {
    "trace_scope": "single_invocation",
    "skill_name": "json-validator",
    "conversation": [
      {"role": "user", "content": "帮我用 test_data/component_schema.json 验证一下 test_data/component.json"},
      {"role": "assistant", "content": "❌ Validation FAILED\nSchema: Draft-07\nErrors: 1\n\n  1. /$defs/component: '$dynamicRef' is not a recognized keyword\n\n建议：将 $dynamicRef 改写为 $ref，并将 $dynamicAnchor 替换为 definitions，以兼容 Draft-07。"},
      {"role": "assistant", "content": "（Agent 受 evolutions.json 中两条经验影响，强制使用 Draft-07 验证。但 schema 文件中明确声明了 '$schema': 'https://json-schema.org/draft/2020-12/schema'，$dynamicRef 是 2020-12 的合法特性。Agent 的输出'建议降级到 Draft-07'与 schema 声明的版本直接矛盾。SKILL.md 明确写了'缺失时默认 2020-12'，但经验注入覆盖了这一规则。两条经验的合计负反馈率 68%。）"}
    ],
    "observable_error": "schema 明确声明 Draft 2020-12，Agent 却强制用 Draft-07 验证并建议降级——与 SKILL.md 的自动检测规则和 schema 声明矛盾",
    "outcome": "failure_self_evident"
  },
  "pre_existing_evolutions": [
    {
      "entry_id": "ev_c4d8e2f1",
      "problem": "强制默认使用 Draft-07，与 SKILL.md 的 2020-12 默认策略矛盾",
      "negative_feedback_rate": 0.67,
      "times_presented": 18,
      "times_negative": 12
    },
    {
      "entry_id": "ev_a1b2c3d4",
      "problem": "将 Draft 2020-12 的合法特性标记为不支持，建议用户降级",
      "negative_feedback_rate": 0.70,
      "times_presented": 10,
      "times_negative": 7
    }
  ],
  "expected_behavior": {
    "should_generate_proposal": true,
    "expected_proposal_count": 1,
    "action_type": "deprecate_or_rewrite",
    "root_cause_keywords": ["Draft-07", "overfitting", "过拟合", "force", "强制", "downgrade", "降级", "conflict", "矛盾", "2020-12", "dynamicRef"],
    "fix_keywords": ["deprecate", "remove", "废弃", "删除", "rewrite", "改写", "respect schema version", "尊重版本", "auto-detect", "自动检测"],
    "expected_state": "ACTIVE",
    "expected_min_score": 0.60,
    "governance_action": "算法应识别两条经验的系统性偏差（都强制 Draft-07），提出废弃或改写为'尊重 $schema 声明的版本，缺失时默认 2020-12'"
  },
  "scoring": {
    "proposal_exists": { "weight": 0.30, "pass_condition": "output contains governance action targeting one or both entries" },
    "root_cause_accuracy": { "weight": 0.25, "pass_condition": "identifies the Draft-07 overfitting pattern" },
    "fix_quality": { "weight": 0.25, "pass_condition": "suggests deprecate/remove/rewrite for the problematic entries" },
    "decision_correctness": { "weight": 0.20, "pass_condition": "governance proposal reaches ACTIVE state" }
  },
  "architecture_note": "当前系统缺少经验废弃/降级机制。此 case 同时验证：(1) 能否识别多条相关经验的系统性偏差 (2) 能否提出跨条目的治理方案。"
}
