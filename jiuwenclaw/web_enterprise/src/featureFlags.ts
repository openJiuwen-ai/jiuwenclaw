/**
 * 前端功能开关（集中管理，便于按构建裁剪 UI）
 */
export const FEATURE_APP_UPDATER_UI = false;

/** 性能模式（agent.fast）是否可在 UI 中切换 */
export const FEATURE_AGENT_FAST_MODE = false;

/** 集群模式（team）是否可在 UI 中切换 */
export const FEATURE_TEAM_MODE = false;

/** 是否展示心跳状态（企业版 K8s 架构暂不展示） */
export const FEATURE_HEARTBEAT_UI = false;
