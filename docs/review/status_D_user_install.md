# 状态核对 D：沙箱用户/密码/install/卸载

核对基准：当前工作区 = HEAD `82001d09`（enterprise_dev_windowbox 分支）。
核对源文件：
- `d:/Workspace/community/jiuwenclaw/jiuwenbox/src/jiuwenbox/supervisor/win_setup.py`
- `d:/Workspace/community/jiuwenclaw/jiuwenbox/src/jiuwenbox/supervisor/daemon_ipc.py`
- `d:/Workspace/community/jiuwenclaw/jiuwenbox/src/jiuwenbox/supervisor/win_constants.py`
- `d:/Workspace/community/jiuwenclaw/jiuwenclaw/agentserver/jiuwenbox_runner.py`（问题 12 涉及）

| # | 问题 | 报告出处 | 当前状态 | 证据 file:line | 说明 |
|---|------|----------|----------|----------------|------|
| 1 | jbx-sandbox 密码硬编码 "000000"；`SANDBOX_USER_PASSWORD_LENGTH=64` 常量定义未用；`_generate_password` 返回固定值 | 5f841f7a/3fe33056/8c7f677a/073d4c1e/82001d09 | ❌ 仍存在 | `win_setup.py:320-327` `def _generate_password() -> str:` 紧接 docstring 后 `return "000000"`；`win_constants.py:36` `SANDBOX_USER_PASSWORD_LENGTH = 64` 仍仅定义，代码中无任何引用 | 与各报告一致，仍是固定弱密码。`_create_sandbox_user` 在 `ret == 2224` 分支注释明说"密码固定, 不重设"（`win_setup.py:356-363`），`_verify_or_reset_sandbox_user_password` 仍重设为该固定密码（`win_setup.py:1203` → `_set_user_password` 传 `password` 来自 `get_sandbox_user_password()`，即注册表存的 "000000"）。生产前必须改随机的诉求未落地。 |
| 2 | `_reg_get_str` 两阶段读修复 DPAPI 密码 hex 截断（原固定 512 chars buffer 截断 → RegQueryValueExW ERROR_MORE_DATA=234 被误当未读到 → get_sandbox_user_password 拿不到密码 → CreateProcessWithLogonW 1326） | 3fe33056 | ✅ 已解决 | `win_setup.py:236-274` `_reg_get_str` 内：第一阶段 `RegQueryValueExW(hkey, name, None, byref(typ), None, byref(size))` 查 size；`ret != 0 and ret != 234: return None`；随后 `char_count = max(1, size.value // 2 + 1)` + `create_unicode_buffer(char_count)` 第二阶段读 | 两阶段读已落地，`ERROR_MORE_DATA=234` 不再被误判为"未读到"。同时改用 `RegOpenKeyExW + KEY_READ_ONLY`（`win_setup.py:245-248`），普通用户读 HKLM 失败走 install 提权路径，KEY_READ_ONLY/KEY_READ_WRITE 拆分到位（`win_setup.py:203-204`）。 |
| 3 | `_set_user_password` 重设 "000000" 撞本地密码复杂度策略 ret=87 | 3fe33056 | ❌ 仍存在（与问题 1 同根） | `win_setup.py:370-393` `_set_user_password` 用 `NetUserSetInfo` 重设为传入的 `password`；调用方 `_verify_or_reset_sandbox_user_password`（`win_setup.py:1203`）传入的 `password` 来自 `get_sandbox_user_password()`，即注册表里 DPAPI 解出的 "000000"。`win_setup.py:355-363` 注释仍保留"重设简单密码 000000 会撞复杂度策略 → ret=87" | 密码仍固定 "000000"，故重设路径仍可能撞复杂度策略 ret=87。注释在 `_create_sandbox_user` 已说明"已存在不重设"以规避首次创建撞策略，但 `_verify_or_reset_sandbox_user_password` 的兜底重设仍用同一弱密码。问题 1 不修则此问题不修。 |
| 4 | `uninstall()` 在 `NetUserDel` 删用户前未确认沙箱 runner 进程已退出；未来"产品卸载"直接调 `uninstall` 而 runner 还在跑会出危险状态（profile 占用删不掉、WFP 已卸但 runner 仍持 token 跑） | ee03da56 | ❌ 仍存在 | `win_setup.py:1244-1288` `uninstall()` 主体顺序：WFP 卸 → `get_sandbox_user_sid()` → `_delete_profile_by_sid(sid_str)` → `_purge_stale_profile_dirs()` → `NetUserDel` → `NetLocalGroupDel` → `_reg_set_str(REG_VALUE_INSTALLED, "")`。**入口无任何"枚举 jbx-sandbox SID 进程"前置检查**；grep `EnumProcesses`/`OpenProcess.*GetTokenInformation` 在 `win_setup.py` 内无命中 | 与 ee03da56 R1 一致：当前调用面（CLI / install 回滚）不触发，但一旦 relay-claw 卸载或 box-server 优雅退出直接调 `uninstall()` 而 runner 还在跑，就会删用户于运行中。报告建议的前置进程枚举检查未实现。`_delete_profile_by_sid` 失败仅 warning 后仍 `NetUserDel`（`win_setup.py:1264-1279`）。 |
| 5 | `_purge_stale_profile_dirs` 用 `shutil.rmtree(ignore_errors=False)`，遇 reparse point 抛 OSError 中止留半删残留 | ee03da56 | ✅ 已解决 | `win_setup.py:521-582`：`shutil.rmtree(path, onerror=_rmtree_onerror)`（`win_setup.py:565`）；`_rmtree_onerror`（`win_setup.py:542-550`）对失败文件 `os.chmod(fpath, 0o777)` 后重试，仍失败 `logger.debug` 跳过不中止；`win_setup.py:568-576` 还有 `os.rmdir` 兜底，删不尽时 warning "留待重启后删" | 与 ee03da56 报告"当前工作树已修，请确认"的预期一致：`_rmtree_onerror` + `os.rmdir` 兜底均已落地，`ignore_errors=False` 不再中止整体清理。 |
| 6 | `_delete_profile_by_sid` 用注册表缓存的 SID，install 早期失败回滚时可能是旧 SID；uninstall 只清 `REG_VALUE_INSTALLED`，`SANDBOX_USER_SID`/`PW`（DPAPI blob）保留；`DeleteProfileW` 失败只 warning 后仍删用户 | ee03da56 | ❌ 仍存在（三处对称缺口均未补） | `win_setup.py:1264-1266` `sid_str = get_sandbox_user_sid()`（读注册表缓存），无 `LookupAccountName` 实时取当前 SID 的兜底；`win_setup.py:1286` 注释"仅清注册表标记 (其他值 SANDBOX_USER_SID/PW 等保留也无害, reinstall 会覆盖)"，`SANDBOX_USER_PW` DPAPI blob 保留；`win_setup.py:1264-1279` `DeleteProfileW` 失败（`_delete_profile_by_sid` 返回 False）仅 warning，紧接仍 `NetUserDel` | ee03da56 R3/R4/R5 三条均未修：仍用注册表缓存 SID、仍只清 installed、DeleteProfile 失败仍继续删用户。在固定密码 "000000" 下"reinstall 会覆盖"的论证勉强成立，但一旦改随机密码（问题 1/3），`SANDBOX_USER_PW` 旧 blob 未清 + install 覆盖不彻底会重现 1326。 |
| 7 | install 回滚机制建立（旧版 best-effort 只 warning、最后无条件写 installed=1 假成功；新版 try/except 致命步骤失败 `uninstall()` 回滚并 re-raise） | e8fb0a69 | ✅ 已解决 | `win_setup.py:888-1027` install 主体包 `try:`（`:893`），致命步骤失败 `except Exception:` 内 `logger.error("install 失败, 执行回滚", exc_info=True)` → `try: uninstall() except: logger.error(...)` → `raise`（`:1019-1027`）；回滚 best-effort 不掩盖原异常。致命/非致命分层清晰：用户+组（`:894-897`）、网络隔离（`:927-960`）致命，隐藏登录/DPAPI/合成 SID/读 ACL 预装非致命 | 与 e8fb0a69 预期一致。回滚机制完整且 `re-raise` 保留原异常，不再假成功。 |
| 8 | `win_constants` 常量错值修正——`SANDBOX_INERT` 0x4→0x2、`UF_DONT_EXPIRE_PASSWD` 0x0200→0x10000、`NetUserDel`/`NetLocalGroupDel` not-found 2201→2221/2220 | e8fb0a69 | ✅ 已解决 | `win_constants.py:69-70` `DISABLE_MAX_PRIVILEGE = 0x1` / `SANDBOX_INERT = 0x2`；`win_constants.py:133` `UF_DONT_EXPIRE_PASSWD = 0x10000`；`win_constants.py:134` `UF_NORMAL_ACCOUNT = 0x0200`；`win_constants.py:140-142` `SANDBOX_USER_FLAGS` 显式 OR `UF_NORMAL_ACCOUNT`；`win_setup.py:1272-1283` `NetUserDel` not-found 用 `2221`，`NetLocalGroupDel` 用 `2220`，注释点明旧版误用 `2201` | 与 e8fb0a69 预期一致，常量值据 `lmaccess.h`/`winnt.h` 修正到位。注：`RESTRICTED_TOKEN_FLAGS = DISABLE_MAX_PRIVILEGE | SANDBOX_INERT` 仍临时去掉 `WRITE_RESTRICTED(0x8)`（`win_constants.py:75`），但这属于受限 token 链路问题，不在本组核对范围内（属 token/隔离组）。 |
| 9 | install 等待无超时，`WaitForSingleObject(event, INFINITE)`，install 子进程在 SetEvent 前崩溃则主进程永久阻塞 | 073d4c1e | ❌ 仍存在 | `win_setup.py:731` `INFINITE = 0xFFFFFFFF`；`win_setup.py:736-738` `wait_result = kernel32.WaitForSingleObject(wintypes.HANDLE(h_event), INFINITE)`；`win_setup.py:751-753` 注释自承"极端: install 中途崩溃, Event 永不 set → 本进程永久阻塞. 加超时兜底"，**但代码里 `INFINITE` 仍是字面量 0xFFFFFFFF，没有改成有限超时** | 与 073d4c1e 报告一致：注释承诺的超时兜底未落地。`_make_install_done_notifier` 的 `finally`（`win_setup.py:1431-1445`）能保证 install 子进程异常退出前 SetEvent，但**子进程被强杀/段错误/OS 崩溃**时不走 finally，主进程仍死等。`agent_ws_server` 把 health check 提到 120s 只是外部杀进程的兜底，不是 install 等待超时。 |
| 10 | install 提权子进程绕过探测——`_load_policy_preinstall_paths` 在 `--force --policy-path` 重装时直接读原始 YAML 不经 `_resolve_tool_paths`，读到 tool_paths 全空 → 预装集丢失工具目录 → 重装后受限 token 读不了 | cafaa1f1 | ❌ 仍存在 | `win_setup.py:585-618` `_load_policy_preinstall_paths(policy_path)`：`with open(policy_path, encoding="utf-8") as f: data = yaml.safe_load(f)` → 直接 `win_fs.get("tool_paths")`（`:600`），**无 `policy_reader.load_policy()` / `_resolve_tool_paths` 调用**；`install()` 在 `policy_path` 分支调用它（`win_setup.py:864-868`）合并预装路径 | 与 cafaa1f1 P1 一致：install 提权子进程仍绕过 `policy_reader._resolve_tool_paths`。基底 YAML 四字段已改空串（靠自动探测填充），但 `--force --policy-path <yaml>` 重装路径读原始 YAML 空 `tool_paths` → 预装集不含工具目录 → 重装后受限 token 读不了 OfficeAce `tools/python`（CreateProcessAsUserW WinError 2/5）。用户显式填了路径再重装也会丢失。 |
| 11 | `daemon_ipc.send_frame` pipe 路径无短写重试（socket 路径用 `sendall`，pipe 路径用 `write+flush` 未循环） | 3fe33056 | ⚠️ 部分解决 / 🔄 以其他方式绕过 | `daemon_ipc.py:103-109` `send_frame(sock, payload)`：`if len(payload) > 0xFFFFFFFF: raise ValueError(...)`；`sock.sendall(struct.pack(">I", len(payload)))`；`if payload: sock.sendall(payload)`。**当前实现已统一用 `sock.sendall`，不再有 `write+flush` 分支** | 3fe33056 报告描述的"socket 路径用 sendall，pipe 路径用 write+flush 未循环"的双路径代码**已不在当前 `daemon_ipc.py` 中**。当前 `send_frame`/`recv_exact` 已无 `getattr(sock, "recv", None) or sock.read` 的 pipe/socket 分叉逻辑（`recv_exact` 也改回纯 `sock.recv`，`:88-100`）。即：pipe 路径的短写隐患**通过"不再支持 pipe 路径"的方式绕过**，而非"显式循环写 pipe"。若 Windows runner roundtrip 仍用 pipe file object，则 `sendall` 在 BufferedWriter 上不存在 → AttributeError。需要确认 Windows 侧是否已改用 socket 而非 pipe（本组未覆盖 `win_exec.py`/`process.py` 的 roundtrip 实现）。标记为"部分解决/绕过"：短写问题不再以原方式存在，但"pipe 路径无循环"的根因未显式修复，只是把 pipe 路径移除了。 |
| 12 | 进程名白名单 `"python"` 过窄，漏杀 uv venv python（`python3.13.exe`/`pythonw.exe`）→ 残留进程清不掉 → `win_proxy` bind WinError 10048 | 8c7f677a | ❌ 仍存在 | `jiuwenbox_runner.py:107` `if proc_name.lower() != "python":` ... `continue`；注释（`:96`）"避免误杀非 python 进程 (win_proxy 只可能由 python 进程占)" | 与 8c7f677a P2 一致：仍是**严格等于 `"python"`**，未改成 `startswith("python")` + 命令行校验。`python3.13.exe`（uv venv）/`pythonw.exe` 会被当非 python 跳过，残留进程清不掉 → win_proxy bind 仍 WinError 10048。 |

## 汇总

本组共核对 **12** 条：

- ✅ 已解决：**4** 条（#2 两阶段读、#5 rmtree onerror、#7 install 回滚、#8 常量错值修正）
- ❌ 仍存在：**7** 条（#1 密码固定、#3 重设撞策略、#4 uninstall 无前置进程检查、#6 uninstall 对称缺口、#9 install 等待无超时、#10 install 绕过探测、#12 进程名白名单过窄）
- ⚠️ 部分解决 / 🔄 绕过：**1** 条（#11 send_frame pipe 短写——通过移除 pipe 路径绕过，未显式循环写）

## 仍存在/部分解决清单（建议优先级）

### 高（生产前必须修）
1. **#1 密码固定 "000000"**（`win_setup.py:327`）：弱密码，任何知晓代码者可 `runas /user:jbx-sandbox`。改 `secrets.token_urlsafe(SANDBOX_USER_PASSWORD_LENGTH)`，`SANDBOX_USER_PASSWORD_LENGTH=64` 常量已就绪。
2. **#3 重设撞密码复杂度策略**（`win_setup.py:1203`）：与 #1 同根，改随机密码即一并解决。
3. **#9 install 等待无超时**（`win_setup.py:731-738`）：注释已承诺超时兜底但代码未实现，`INFINITE` 改 `120_000` ms 即可，超时降级 warning。
4. **#10 install 绕过探测**（`win_setup.py:585-618`）：`_load_policy_preinstall_paths` 改走 `PolicyReader.load_policy()` 或抽公共"加载+探测"函数，保证 install 与 runtime 读同一份填充后 tool_paths。

### 中（应修）
5. **#4 uninstall 无前置进程检查**（`win_setup.py:1244-1288`）：入口加"枚举 jbx-sandbox SID 进程"前置检查，或在 docstring/调用约定强约束"调用方必须先停所有沙箱 runner"。
6. **#6 uninstall 对称缺口**（`win_setup.py:1264-1288`）：uninstall 顺手清 `REG_VALUE_SANDBOX_USER_SID`/`SANDBOX_USER_PW`；`_delete_profile_by_sid` 调用前 `LookupAccountName` 实时取当前 SID，注册表缓存作 fallback；`DeleteProfileW` 失败时 abort 删用户或显式 warning 提示残留。在改随机密码（#1）后这两条会重现 1326，建议与 #1 同批处理。
7. **#12 进程名白名单过窄**（`jiuwenbox_runner.py:107`）：改 `proc_name.lower().startswith("python")` + 命令行含 `jiuwenbox`/`win_proxy` 校验。

### 低（绕过/部分解决）
8. **#11 send_frame pipe 短写**（`daemon_ipc.py:103-109`）：当前已统一 `sock.sendall`，pipe 路径已移除。需确认 Windows runner roundtrip 确实已改用 socket（非 pipe file object），否则 `sendall` 在 BufferedWriter 上 AttributeError。建议显式循环写或确认无 pipe 调用方。
