# GitCode OAuth 应用配置
# 在 GitCode → 个人设置 → OAuth 应用 中创建应用后填写
# 回调地址请填写：http://localhost:5173/oauth/callback （开发环境）
VITE_GITCODE_OAUTH_CLIENT_ID=
VITE_GITCODE_OAUTH_CLIENT_SECRET=

# GitHub OAuth App 配置
# 在 GitHub → Settings → Developer settings → OAuth Apps 中创建应用后填写
# 回调地址请填写：http://localhost:5173/oauth/callback （开发环境）
# 注意：GitHub 和 GitCode 可共用同一个回调路径，前端会根据 active provider 自动区分
# 注意：Client Secret 不应放在前端环境变量中，token 兑换应通过服务端代理完成
VITE_GITHUB_OAUTH_CLIENT_ID=
