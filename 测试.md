# 工具检索 v3（BM25 + CJK 分词）测试报告

> 分支 `feat/tool-retrieval-v3` | 测试日期 2026-08-13
> v3 是 v2 PR（MR 4781）开会后的改进方案：砍掉 fastembed（90MB 模型太大无法落地），改用 **BM25 + CJK n-gram 分词**单扛中文，零依赖零模型零体积。对标 ratel 官方默认策略（BM25 默认，语义 opt-in）。

---

## 一、122 条真实 query 离线测评（v3 vs v2）

**数据来源**：8/4-8/5 抓取的 122 条 LLM 真实 `search_tools` query（来自 `~/.jiuwenswarm/agent/.logs/prompt_capture/`）。
**方法**：用 35 个真实工具的中文描述建 BM25+CJK 索引，喂这 122 条真实 query，判命中，并与 v2 当时实际返回对比。

| 指标 | v3（BM25+CJK） | v2（当时实际） |
|---|---|---|
| **top-k 命中率** | **104/118 = 88%** | 45/118 = 38% |
| **top-3 命中率** | **107/118 = 91%** | — |
| **空结果率** | **0/122 = 0%** | 57/122 = 47% |
| v2 错 → v3 修对 | **64 条** | — |
| v2 对 → v3 弄坏 | 5 条 | — |

**结论**：v3 命中率从 38% → **88%**（+50 个百分点），空结果从 47% → **0%**，净修复 59 条。CJK 分词根治了 v2 中文整段当 token 导致的静默漏召回。

**v3 仍错的 14 条**分两类，均非算法根因：
- **cron 家族内部排序（8 条）**：query 写"创建定时任务 cron"期望 `cron` 外壳工具，v3 返回 `cron_create_job`——语义上正确（能干活），是期望标注口径问题。
- **工具不存在（`powershell 搜索引擎` 等）**：LLM 想找的能力工具集没有，v3 返回语义最近的——属工具集缺失，非检索算法问题。

**典型修复样例**：
- `发送文件给用户 send file` → v2 返回 `write_file`（字面"file"撞车）→ v3 返回 `send_file_to_user` ✓
- `task_tool for delegating` → v2 返回 `load_tools` → v3 返回 `task_tool` ✓

---

## 二、500 工具规模测试（几百工具 + MCP 场景）

**目的**：回答"加几百个工具含 MCP 工具还能 search 到吗"。用真实 35 工具 + 465 个领域干扰工具（git/docker/k8s/pdf/excel 等中文描述）= 500 个，测命中和延迟。

| 指标 | 结果 |
|---|---|
| 工具总数 | 500 |
| 建索引 | 18 ms |
| **查询延迟** | **0.7 ms（平均）** |
| top1 命中 | 9/12 |

**性能结论**：500 工具查询 0.7 毫秒，纯内存计算，和 ratel 一样"in-process, deterministic"。BM25 查询复杂度 = query词数 × 工具数，几百几千工具都是毫秒级。**性能不是瓶颈。**

**按 query 形态的命中规律**：
| query 形态 | 500 工具下命中 |
|---|---|
| 带工具名（`cron_create_job 创建定时任务`）| **100%**（字面精确）|
| 带英文领域词（`docker 容器镜像`）| 基本能（描述雷同会撞，排序可能偏）|
| 纯中文同义词（`帮我建个定时任务`）| 退化（大语料干扰多，top1 易错，top3 多半有）|

**短板**：纯中文同义词在大语料里命中率下降——这正是 dense（语义层）的价值点，但需模型，v3 暂不启用。

---

## 三、动态注册测试（新增 tool / MCP tool 能否被 search + 使用）

**目的**：回答"用户新加工具、新挂 MCP server，新工具能被检索到并调用吗"。
**方法**：模拟 v3 的三层机制（`dynamic_register_test.py`，复刻 sig 缓存 + name 直查 + force_refresh 逻辑）。

### 三层机制

| 层 | 机制 | 触发时机 |
|---|---|---|
| 第 1 层 | `before_invoke` 每轮全量刷新 `ability_manager.list_tool_info()` + 重建索引（sig 变了才建）| 下一轮对话开始 |
| 第 2 层 | `_lookup_tool_by_name`：query 像工具名（含下划线）时实时从 ability_manager 取 | 本轮，query 带工具名 |
| 第 3 层 | `_force_refresh_and_retry`：BM25 搜空 → 比对真实工具集 vs 缓存 → 变了就重建再搜 | BM25+name 都空时 |

### 四个场景结果

**场景 A：本轮新工具 + query 带工具名**
```
query = 'image_generate 生成图片'          （索引未刷新）
[第1层 BM25] → ['send_file_to_user', ...]  （未命中新工具）
[第2层 name直查-实时全量] → 命中 image_generate ✓（按名直调，不依赖索引）
```
✅ **name-based 直调**：工具只要在 ability_manager 注册，search 返回定义后 LLM 按名直调，不依赖 prefill tools[] 列表、不依赖索引刷新。

**场景 B：本轮新工具 + 纯中文 query（不带工具名）**
```
query = '生成一张图片'                      （索引未刷新）
[第1层 BM25] 旧索引(35) → ['send_file_to_user', ...]  （未命中）
[第2层 name直查] query无下划线不像工具名 → 不触发
[第3层 force_refresh] 检测工具集变化(35→38) → 重建索引 → 重搜: ['image_generate', ...]
  ✓ 新工具进索引后搜到: image_generate
```
✅ 搜空触发 force_refresh 自动重建索引补搜。

**场景 C：下一轮 before_invoke 自动刷新（主路径）**
```
本轮索引 35 个（无新工具）→ 下一轮 before_invoke: 工具集变化 → 重建
现在索引 38 个（含新工具）
query '帮我生成一张图片' → ['image_generate', ...] ✓命中新工具
query '在github提个issue' → ['mcp_github_create_issue', ...] ✓
query '查数据库 postgres' → ['mcp_postgres_query', ...] ✓
```
✅ 新工具下一轮自动进索引，BM25 正常搜到，纯中文也能命中。

**场景 D：MCP server 批量挂载（5 个 slack 工具）**
```
本轮索引 38（无 slack）→ query 'mcp_slack_send_message 发slack消息'
  [BM25] → ['send_file_to_user', ...]（没进索引）
  [name直查-实时全量] → mcp_slack_send_message ✓（按名直调）
下一轮 before_invoke 刷新后索引 43 个
  query '发slack消息到频道' → ['mcp_slack_create_channel', ...] ✓
  query 'mcp_slack_list_channels 列频道' → ['mcp_slack_list_channels', ...] ✓
```
✅ MCP 批量挂载同上：本轮带名直查/下轮进索引。

### 动态注册结论

**新工具 / MCP 工具能被 search 到、能被使用。**
- 带工具名 query：本轮即可用（name 直查 + 按名直调）
- 纯中文 query：搜空时 force_refresh 自动重建补搜；下一轮 before_invoke 必进索引
- **唯一窄缝**：本轮新工具 + 纯中文 query + BM25 撞了别的工具没搜空（不触发 refresh）→ 本轮漏，下一轮 before_invoke 自愈。极罕见，且真实 LLM query 大多带工具名（122 条数据印证），实战基本撞不上。

---

## 四、真实对话实测（capture + 真 MCP server）

### 4.1 内置工具真实对话
在 `search_tool_rail.py` 临时加轻量 capture，跑 DeepAgent 新对话：
```
query='cron_create_job 创建定时任务'    → cron_create_job ✓
query='send_file_to_user 发送文件给用户' → send_file_to_user ✓
query='free_search 免费网页搜索'        → fetch_webpage（字面歧义软误中，LLM 二次搜索自纠）
query='free_search 免费搜索'            → free_search ✓
```
4 条全走 main 路径（BM25 直接命中），0 空结果。印证真实场景 query 多为"工具名+中文"混合形态。

### 4.2 真实 MCP server 实测（filesystem）
往用户 config（`~/.jiuwenswarm/config/config.yaml`，不是仓库默认 config）的 `mcp.servers` 挂了一个真实 MCP server：
```yaml
mcp:
  servers:
    - name: filesystem
      enabled: true
      transport: stdio
      command: npx
      args: ["-y", "@modelcontextprotocol/server-filesystem", "D:\\work\\program\\jiuwen"]
```
重启服务后日志确认：**MCP server 挂载成功**，工具数 36→52（多 16 个 `mcp_filesystem_*`），`executable corpus: 52/52 (filtered 0 ghosts)`——MCP 工具全部进 BM25 索引、未当 ghost 滤掉。MCP 工具命名规则：`mcp_<server名>_<工具名>`。

**但实测暴露三个问题：**

**问题 1（已修复）：MCP 工具描述是英文，中文 query 搜不到**
MCP 工具描述全英文（`mcp_filesystem_list_directory` = "Get a detailed listing of all files and directories..."），内置同类工具是中文（`list_files` = "目录列表 列出指定目录下的文件和子目录"）。中文 query 下，中文描述工具词频分高，英文描述 MCP 工具被压后。

**问题 2（已修复）：BM25 路径缺 name 精确匹配加权**
更严重——`search_tools("mcp_filesystem_list_allowed_directories 查看允许访问的目录")` 带**完整工具名**却返回 `skill_tool`（前端实测复现）。根因：`bm25_search` 纯靠词频，不像 `dense_search`（`search.py:118`）对"query 含工具名"做 `sim += 1.0` 加权。CJK bigram 词频放大效应让中文描述的内置工具盖过了工具名精确命中。离线打分实证：
```
query='mcp_filesystem_list_allowed_directories 查看允许访问的目录'
   3.596  list_files                          ← 中文描述撞中文 query，词频赢
   0.742  mcp_filesystem_list_allowed_directories ← 带名都输
```

**修复（已实现、109 单测无回归）**：给 `BM25Index.search` 加 name-match boost 层（对齐 dense 路径 `search.py:118-132` 的 exact/contains/prefix/token-overlap 分层）。关键设计点：
- boost 量级**缩放到该轮最高 BM25 分**（IDF 尺度，不是 cosine 的固定 +1.0），保证 name 命中能压过词频失配。
- token-overlap 只认**长 token（≥5 字符）且完整词相等**，不算子串——修了 `list`/`files` 这种通用短词误撞（`list_files` 的 `list` 曾误中 `mcp_filesystem_list_...` 而被错误 boost）。
- 对 BM25=0 的工具（零词重叠）也用 name-boost 救回——否则纯中文 query 搜英文描述 MCP 工具，工具名命中了也因 0 分不可见。

**修复后实测（capture）**：
```
query='mcp_filesystem_list_allowed_directories 查看允许访问的目录' → mcp_filesystem_list_allowed_directories ✓（之前 skill_tool）
query='mcp_filesystem_create_directory 创建目录'                  → mcp_filesystem_create_directory ✓
query='mcp_filesystem_list_directory 列出目录内容'                → mcp_filesystem_list_directory ✓
```
三个带名 MCP 工具 case 全部 top1 命中。

**问题 3（待定，未修复）：MCP 工具在对话里"隐身"，LLM 默认退回 bash**
前端开新对话说"创建 rick 目录"，LLM **用了 bash 的 mkdir，没去搜/调 `mcp_filesystem_create_directory`**。根因：MCP 工具描述是英文 + `_hidden_tool_category`（`search_tool_rail.py:821`）分类逻辑里 `mcp_filesystem_*` 不匹配任何关键字，落到笼统的 `"other"`（"其它（专业工具）"）。LLM 看到一堆英文工具名列在"其它"类下，难以关联到"能创建目录"这个具体能力，退回更熟悉的 bash。

这问题比检索排名更影响实际使用：**用户不会总带工具名搜**，LLM 得先"知道有这工具、且理解它能干什么"才会用。涉及 prompt 工程（分类 + 摘要呈现），改代码不一定立刻见效，待和"要不要把 MCP 工具描述中文化"一起定方向。

### 4.3 第三个问题：MCP 工具"隐身"的根因与修复验证

**导航摘要实测确认**（加临时日志打 `cn_text`）：MCP 工具**确实进了导航摘要**，但全堆在笼统的"其它（专业工具）"类、描述英文：
```
- 其它（18 个）：专业工具
  - mcp_filesystem_create_directory：Create a new directory or ensure a directory exists...
  - mcp_filesystem_directory_tree：Get a recursive tree view...
  ...（16 个英文工具堆一起，LLM 关联不到具体能力）
```

**方向 A（已实现）：改 `_hidden_tool_category` 按 MCP server 归类**
- `mcp_<server>_<action>` → 归到 `mcp_<server>` key（不再落 "other"）
- 渲染时动态生成标签：`MCP: filesystem（16 个）：MCP filesystem server 提供的工具，按需调用`
- 109 单测无回归

**但方向 A 单独不够**——改完分类后，filesystem 实测 LLM **仍退回 bash**。根因不是分类，是两个更深的因素：
1. **能力可替代性**：filesystem 工具（read/write/list/tree/stat）都能被 bash 或内置工具替代 → LLM 选更熟悉的 bash
2. **描述语言**：MCP 工具描述英文，中文对话里 LLM 关联弱

**dice MCP 实测（根因坐实）**：写了一个最小自定义 MCP server（`mcp_dice_server.py`），暴露 `roll_dice`/`get_roll_history`——能力**独特**（跨会话持久化掷骰历史，bash `$RANDOM` 做不到）+ 描述**中文**。挂载后新开对话，用户纯自然语言"帮我掷一个20面的骰子，掷3次"（不带工具名）：
```
LLM → search_tools("mcp_dice_roll_dice 掷骰子") → 命中 mcp_dice_roll_dice ✓
LLM → 按 name 直调 mcp_dice_roll_dice(sides=20, count=3)
     → "掷 20 面骰子 3 次，点数：[10, 15, 10]，总和：35" ✓
```
LLM **主动 search 发现 + 调用 MCP 工具，没退回 bash**。注意 LLM 的 query 带了工具名 `mcp_dice_roll_dice`——说明它从导航摘要里看到了该工具名（方向 A 归类让它进了 `MCP: dice` 类被注意到），间接证明方向 A 有用。

| 对照实验 | 描述语言 | 能力可替代 | LLM 是否用 MCP 工具 |
|---|---|---|---|
| filesystem | 英文 | 是（bash 能干）| ❌ 退回 bash |
| dice | 中文 | 否（持久化历史）| ✓ search + 调用 |

**结论**：第三个问题根因 = 能力可替代 + 描述英文。方向 A（分类）是正确改进（保留），但要让 LLM 用 MCP 工具，关键在"能力独特 + 描述中文化"。

### 4.4 新增 tool / MCP tool 的检测能力总结（已证实）

经 filesystem + dice 两组真实 MCP server 实测，**新装的 tool / MCP tool 都能被检测到**：

| 检测环节 | 结果 | 证据 |
|---|---|---|
| MCP server 挂载 | ✓ 成功 | 注册工具数 36→52（filesystem）/ →38（dice），`executable corpus: 全部/全部 (0 ghosts)` |
| 进 ability_manager | ✓ | `registered tools (N)` 日志含 `mcp_*` |
| 进 BM25 索引 | ✓ | 0 ghost 滤除，全进语料 |
| 带工具名 search | ✓（name boost 修复后）| dice 实测 `mcp_dice_roll_dice 掷骰子`→命中 |
| 自然语言 search + 调用 | ✓ | dice 实测"掷骰子"→search→命中→调用成功 |
| LLM 主动发现（导航摘要）| ✓ | dice 实测 LLM 从摘要看到工具名，query 带名 |

**前提条件**：能力独特（不被 bash/内置替代）+ 描述中文化时，LLM 会主动 search 发现并调用 MCP 工具。能力可被替代且描述英文时，LLM 倾向退回 bash（非检索失败，是 LLM 决策偏好）。

---

## 五、对标 ratel

| 维度 | ratel 官方 | v3 |
|---|---|---|
| 默认检索 | BM25，no model | BM25+CJK，dense 默认关 |
| BM25 参数 | k1=0.9, b=0.4 | 同（照搬 ratel）|
| haystack 投影 | schema-aware，排除 type/required | 同（照搬 ratel indexing.rs::searchable_text）|
| 中文/多语言 | bge-small-en，"queries in other languages rank poorly"，要换 multilingual 模型 | **CJK n-gram**让 BM25 不下模型也能检索中文（ratel 没做）|
| 语义层 | opt-in，bge-small-en，下载 ~130MB | 砍掉本地模型；可改云端 API（ratel 也支持 OpenAI-compatible endpoint）|
| hybrid 融合 | RRF k=60 | 同（照搬 ratel fusion.rs）|

**v3 = ratel 推荐的默认策略 + CJK 中文增强**。方向正确。

---

## 六、单测

`tests/unit_tests/common/test_tool_retrieval.py` 等 3 个测试文件，109 个用例全过（BM25 参数、boost 分层、verb-intent 路由、haystack、summary 各 detail level、ghost 过滤、dense 排序、0-drift 字节级比对）。

---

## 七、总结与建议

| 验证项 | 结果 |
|---|---|
| 122 条真实 query 命中率 | v3 88% vs v2 38%，空结果 0% vs 47% |
| 500 工具规模 | 延迟 0.7ms，性能无瓶颈 |
| 动态注册（新工具/MCP）| 三层兜底，能 search 能调用 |
| 真实 MCP server 实测 | filesystem(16 工具) + dice(2 工具) 都挂载成功、进索引 |
| BM25 name-boost 修复 | 带名搜 MCP 工具 3/3 命中（修复前 0/3）|
| 新工具/MCP 可被检测 | ✓ 已证实（见 4.4 / 八）——英文描述照样被搜到，前提不是"描述中文化" |
| MCP 工具分类修复（方向A）| 按 server 归类，不再堆"其它"，109 单测无回归 |
| MCP 调用层参数序列化修复 | ✓（见八）——str→array/object 容错，memory array 参数调用成功 |
| 单测 | 109/109 通过，修复无回归 |

**结论**：v3（BM25 + CJK 分词）+ name-boost 修复 + MCP 分类修复 + MCP 参数序列化修复验证通过，可固化提交、往 v2 PR（MR 4781）上提。**新装的 tool / MCP tool 都能被检测到、能被 search、能被调用**（见八：英文描述 + name-boost 兜底 + 参数容错，三 MCP 实测端到端跑通）。

**已修复**：
1. BM25 路径缺 name 精确匹配加权——对齐 dense 路径，带工具名 query 现在能命中 MCP/英文描述工具（实测 0/3 → 3/3）。
2. MCP 工具分类笼统——按 server 归类（`mcp_<server>`），动态生成标签 `MCP: <server>`，不再堆"其它（专业工具）"。
3. **MCP 调用层参数序列化**（8/14）——LLM 把 array/object 参数序列化成 str 传入，`format_with_schema` 不反序列化直接抛 `Input should be a valid list [input_type=str]`。patch `MCPTool.invoke` 在 validate 前 str→list/dict（见八）。

**已知短板（根因已坐实）**：
1. ~~MCP 工具 LLM 退回 bash = 描述英文~~（8/14 证伪）：dice 描述改英文照样搜到+调用成功（name-boost 兜底）。filesystem 退 bash 真根因是**能力可被 bash 替代 + bash 更顺手**（mkdir 比 MCP 顺手），LLM 退 bash 是正确决策不是 bug。**描述语言不是根因**，中文化方向已停。
2. 纯中文同义词在大语料里 top1 易错（top3 有解）——dense（云端 embedding）可救，作为可选增强。
3. cron 家族内部排序口径问题——非算法根因。
4. 工具不存在的死循环（`free web search` 搜 50 次）——工具集缺失，非检索问题。

**后续可选**：
- 语义层：复用项目已有 `embed:` 配置 + openjiuwen 的 `OpenAIEmbedding`/`DashscopeEmbedding`，云端 embedding + 预计算 index（KB 级），ratel 官方背书此路。dense 默认关，opt-in 开 hybrid。
- Dolores 移植：v3 检索目前只在主线 DeepAgent 挂着，DoloresAgent 线（`extensions/dolores/.../interface_deep.py` 硬编码 `progressive_tool_enabled=False`）未接，需照搬主线 3 处 + 解决 `react_agent` 兼容性。（param coerce patch 已顺手在 dolores 接好，等 v3 检索移植过去即生效。）
- ~~MCP 工具描述中文化~~（8/14 证伪停掉，见八）。

---

## 八、8/14 MCP 实测：证伪"中文化" + 修调用层参数序列化（todo1 闭环）

> 测试日期 2026-08-14，分支 `feat/tool-retrieval-v3`。承接 4.3/4.4，用三个真实 MCP server 把"MCP 工具能不能搜到 + 能不能调通"彻底测透。

### 8.1 起因：4.3 的"描述英文是根因"判断要验证

4.3 对照实验（filesystem 退 bash vs dice 成功）当时下结论"根因 = 能力可替代 + 描述英文"，并立了"MCP 工具描述中文化"治本方向（已写进旧版七）。8/14 逐个验证这个判断——**证伪了"描述英文"这条**。

### 8.2 三个 MCP server 实测（全英文描述）

| MCP 工具 | 描述 | 参数形态 | 检索 | 调用 | 真正失败/成功点 |
|---|---|---|---|---|---|
| dice（描述改英文后）| 英文 | 简单 int | ✅ 搜到（LLM query 带名 `mcp_dice_roll_dice`）| ✅ 成功 | name-boost 兜底，英文照样搜到+调通 |
| filesystem | 英文 | 简单 str | ✅ 搜到（query 带 `mcp_filesystem_create_directory`）| ❌ 失败 | 路径范围（server 限 `D:\work\program\jiuwen`，LLM 建在 workspace 下被拒）+ **能力可被 bash 替代（mkdir 更顺手，退 bash 是正确决策）** |
| memory（官方 `@modelcontextprotocol/server-memory`）| 英文 | **array** | ✅ 搜到（纯中文"记住我偏好"→LLM 自搜 `mcp_memory_create_entities`）| ❌→✅ | **参数序列化 bug**：array 被传成 str，patch 后成功 |

**关键观察**：三个全英文描述的 MCP 工具，检索层**全部搜到**。LLM 的 `search_tools` query 几乎都自带工具名（从导航摘要 `MCP: <server>` 类记住了名字）→ name-boost 保底命中。**"英文描述搜不到"这个假设在实测中从未发生。**

### 8.3 证伪：描述中文化没必要做

- dice 描述改成纯英文后，照样 search 到 + 调用成功（8.2 第一行）→ 推翻"dice 成功是因为描述中文"
- filesystem 退 bash 真根因是"能力可替代 + bash 更顺手 + 路径范围"，不是描述英文
- memory 是英文描述被纯中文意图搜到 → 中文化要治的"搜不到"根本没出现

**中文化 plan（旧七的治本方向）前提站不住，停掉。**`cn_desc.py` 不写。三个案例没有一个需要"中文化"来救。

### 8.4 真根因 + 已修：MCP 调用层参数序列化 bug

memory 实测暴露的真问题——LLM 传 `entities='[{"name":"用户偏好"...}]'`（**字符串**），但 schema 要 `array`，openjiuwen 报：
```
Input should be a valid list [type=list_type, input_value='[{"name": "用户偏好"...}]', input_type=str]
```

链路：`MCPTool.invoke`（`openjiuwen/core/foundation/tool/mcp/base.py:166`）→ `SchemaUtils.format_with_schema`（`schema_utils.py:24`）→ `validate_with_schema`（`:49`）直接抛 ValidationError。**`format_with_schema` 不做 str→list/dict 反序列化**。

这是**通用 bug**：影响所有参数含 array/object 的 MCP 工具（memory entities / filesystem paths / github labels…），不是 memory 独有。检索层一直通（搜到），断在调用层。

**修复**：新建 `jiuwenswarm/common/mcp_param_coerce_patch.py`，照 `mcp_call_timeout_patch.py` 模式 patch `MCPTool.invoke`：
- validate 前遍历 `inputs`，对 schema 声明 `type:array`/`type:object` 但值是 str 的字段，`json.loads` 转回 list/dict
- **只转 array/object 类型字段**（string 字段绝不动，避免误转存 JSON 文本的合法 string）
- 非 JSON 字符串不转（留原样让原校验报错，**不掩盖真问题**）
- parse 出来类型不匹配也不转
- **只 patch `MCPTool.invoke`，不碰共享 `SchemaUtils`**（它有 6 个调用点：function/restful/llm/workflow，动了风险大）
- 幂等（`_PATCHED` 守卫）
- 主线 + dolores 两条 `interface_deep.py` 都从 `__init__` apply（dolores 等 v3 检索移植过去即生效）

### 8.5 验证

- 单测：`test_tool_retrieval.py` 95/95 通过，无回归
- patch 逻辑单测：memory `entities` str→list ✅；非 JSON 字符串保留 str（不掩盖）✅；string 字段不动 ✅；已是 list 不变 ✅
- 真实 MCP 实测：memory `mcp_memory_create_entities`（array 参数）调用成功，LLM 不再退回 bash/内置。**端到端跑通：纯中文意图 → LLM 自搜英文描述 MCP → 按名调 → array 参数正确处理 → 调用成功**

### 8.6 todo1 闭环

|MCP 工具 | 参数形态 | 检索 | 调用 |
|---|---|---|---|
| dice | 简单 int | ✅ | ✅（8/13）|
| memory | array | ✅ | ✅（8/14，param coerce patch 后）|

**任意参数形态的 MCP 工具都能搜到 + 调通**——todo1（工具检索 v3）在主线 DeepAgent 上的闭环闭合。检索层（搜到）+ 调用层（参数容错）都通，英文描述无需中文化。

### 8.7 改动清单（8/14，未 commit）

| 文件 | 改动 |
|---|---|
| `mcp_dice_server.py` | dice 描述改英文（验证"英文照样搜到"，保留）|
| `jiuwenswarm/common/mcp_param_coerce_patch.py` | **新增**，patch MCPTool.invoke 做 str→array/object 容错 |
| `jiuwenswarm/server/runtime/agent_adapter/interface_deep.py`（主线）| import + apply param coerce patch |
| `jiuwenswarm/extensions/dolores/server/runtime/agent_adapter/interface_deep.py`（dolores）| import + apply param coerce patch |
| `~/.jiuwenswarm/config/config.yaml` | 摘掉 filesystem、加 memory server（验证用）|

**下一步**：commit 这批改动 → todo2（Dolores 移植 v3 检索）。
