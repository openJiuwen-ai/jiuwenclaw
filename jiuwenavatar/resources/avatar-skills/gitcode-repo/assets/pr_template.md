<!--
═══════════════════════════════════════════════════════════════════
  GitCode PR/MR 正文模板（供 Agent 填写）
  路径：skills/gitcode-repo/assets/pr_template.md
═══════════════════════════════════════════════════════════════════

【使用方式】
0. **Aidlc 流水线 G7b 必须复制本文件**为 `pr-body.md`（须 **G7a PASS** 后再执行）（见 `references/pr_guide.md` 与各平台 `dev-leader` skill）；**必须**填写「审查摘要」与「文档与计划」中的审查结论（摘自 `doc/<module>/review.md`）。
1. **独立 PR / 社区规范任务**：同样可复制本文件为临时正文，逐项替换【待填写】占位符（审查摘要节可删或写「不涉及」）。
2. 发布前删除本 HTML 注释块，以及各节「> 填写指引」引用块（勿提交到 PR 正文）。
3. 用 `pr_creator.py --create --body-file pr-body.md` 创建 PR；创建前先 `--list --head <branch>` 查重。
4. **仅**在 `skills/gitcode-repo/` 生成 `pr-body.md`（禁止 `pr_body.md`、禁止在业务仓 `repo-root`）；**`--create` 成功或 `--dry-run` 正常结束后删除**；**`--create` 失败时保留**以便重试，勿 commit。
   完整流程见 `references/pr_guide.md`。

【标题建议】（写在 PR title，非正文）
  <module>: <动词>简要描述（**说明性文字须简体中文**）
  示例：web-config: 修复清空 Agent/Team 后无法保存
  或使用 Conventional Commits：fix(web-config): 允许保存时 Team 为空

【填写原则】

- **语言（硬约束）**：标题说明与正文各节叙述 **必须使用简体中文**；代码/标识符/日志原文等「特定内容」可保留英文（见 `references/pr_guide.md`「正文语言」）。
- Aidlc G7b：**必须**使用本模板；**必须**从 `doc/<module>/review.md` 摘录审查摘要（Gate Verdict、Must Fix 闭环、合并建议、关键验证建议）。
- 正文须可复核；只写已执行的验证结果；`/kind` 须与变更性质一致。

【不适用章节】整节删除或写「不涉及」，勿留空标题。
═══════════════════════════════════════════════════════════════════
-->

## 元信息（派工与追溯）

> **填写指引**：便于 Leader 与 Maintainer 对照 Issue、文档链与分支。

| 字段 | 值 |
|------|-----|
| **module** | 【待填写，如 `web-config`，与 `doc/<module>/` 一致】 |
| **关联 Issue** | 【#编号 或 无；正文下方也须写 Closes/Fixes】 |
| **head 分支** | 【待填写，如 `feature/web-config-fix`】 |
| **base 分支** | 【待填写，如 `develop`；与 `gitcode-repo.json` 一致】 |

---

## 变更类型

> **填写指引**：保留 openJiuwen 社区规范。将下方 `/kind <label>` **整行替换**为唯一一种（删除 `<label>` 占位符）。PR 描述不符合规范时，修改后可能需 `/check-pr` 重新检查。

**What type of PR is this?**

/kind 【待填写：bug | task | feature | refactor | clean_code 之一】

| Issue 类型 | 建议 `/kind` |
|--------------------|--------------|
| Bug 修复 | `bug` |
| 新功能 | `feature` |
| 重构 | `refactor` |
| 文档/杂项清理 | `task` 或 `clean_code` |

---

## 变更摘要

> **填写指引**：2–5 条要点，说明**用户可见变化**或**修复的问题**；避免只列文件名。

- 【待填写】
- 【待填写】

---

## 关联 Issue

> **填写指引**：合并后应关闭 Issue 时写 `Closes #N`；仅关联不关闭用 `Related to #N`。

Closes #【待填写编号，无则写「无」并删除本行格式】

---

## 实现说明

> **填写指引**：关键设计/实现选择、影响模块、对外接口或配置变更；无接口变更可写「无对外接口变更」。

【待填写：做了什么、为什么这样做；可附主要文件路径】

---

## 文档与计划

> **填写指引**：若本次变更经过需求→设计→开发→测试文档链，**必填**；小改动可删本节并注明「跳过文档链」。

| 产物 | 路径 / 状态 |
|------|-------------|
| requirements.md | 【待填写：已更新 / 不涉及】 |
| design.md | 【待填写】 |
| dev_plan.md | 【待填写：相关任务已勾选 [x] / 不涉及】 |
| test_plan.md | 【待填写：相关项已勾选 [x] / 不涉及】 |
| 审查结论 | 【待填写：摘自 `doc/<module>/review.md` 的 Gate Verdict，如 PASS / REWORK / HOLD】 |

---

## 审查摘要（Aidlc G7b 必填）

> **填写指引**：从 `doc/<module>/review.md` 摘录，勿空泛。至少含：Overall Verdict / Gate、Must Fix 是否已闭环、合并建议（Yes/No/Block）、1–3 条关键验证建议。

【待填写：审查摘要正文】

---

## 验证结果

> **填写指引**：**必填**。写已执行的命令、环境、结果摘要；Bugfix 须对照 Issue 期望结果说明是否达成。

### 功能 / 回归验证

【待填写：步骤 + 实际结果，如「清空 Agent 后保存成功，无 `team must be a non-empty array`」】

### 自动化测试

```text
【待填写：执行的测试命令，如 pytest path -q】
```

【待填写：通过 / 失败 / 未执行及原因】

### 手动 / UI 验证（如适用）

【待填写或「不涉及」】

---

## 自检清单（Self-checklist）

> **填写指引**：按**实际情况**在 `[ ]` 内打 `x`（`[x]`）。未评审、未测试、未验证的项**禁止**勾选。社区要求四项均需 Maintainer 可检视。

**Self-checklist**：（请自检，在 `[ ]` 内打上 x）

- [ ] **设计**：PR 对应的方案是否已经过 Maintainer 评审，方案检视意见是否均已答复并完成方案修改
- [ ] **测试**：PR 中的代码是否已有 UT/ST 测试用例充分覆盖，新增测试用例是否随本 PR 一并上库或已经上库
- [ ] **验证**：PR 描述中已包含对该 PR 对应 Feature / Refactor / Bugfix **预期目标达成情况**的详细验证结果（见上文「验证结果」）
- [ ] **接口**：是否涉及对外接口变更；如有，变更已通过接口评审，API 注释已同步更新（不涉及则保持未勾选并在「实现说明」注明）
- [ ] **文档**：是否涉及官网文档修改；如有，已提交 Doc 仓资料（不涉及则保持未勾选）

---

## 审阅者注意事项（可选）

> **填写指引**：兼容性、依赖升级、性能、安全等需 Maintainer 重点看的点；无则删除本节。

- 【待填写，如：是否影响前向兼容、三方库版本变更等】

---

## 补充说明

> **填写指引**：风险、回滚方式、上线注意、截图/日志链接；无则删除本节。

【待填写】
